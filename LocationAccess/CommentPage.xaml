<Page
    x:Class="LocationAccess.CommentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LocationAccess"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock Margin="14,10,0,0" x:Name="Secret"></TextBlock>
        <ListView Margin="14,60,0,0" x:Name="CommentList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel  Width="300">
                        <TextBlock Margin="14,10,0,0" Text="{Binding content}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Margin="14,450,0,0" >
            <TextBox Margin="14,0,100,30" x:Name="textComment"></TextBox>
            <Button Content="Post" Click="Button_Click"></Button>
        </Grid>
    </Grid>
</Page>
