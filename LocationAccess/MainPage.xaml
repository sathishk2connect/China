<Page
    x:Class="LocationAccess.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LocationAccess"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.BottomAppBar>
        <CommandBar>
                

            <AppBarButton Icon="Add" Label="Add">
                <AppBarButton.Flyout>
                    <Flyout>
                        <Grid Height="400">
                            <ListBox x:Name="Add" />
                        </Grid>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
        </CommandBar>
    </Page.BottomAppBar>
    <Grid>
        <Button Content="Get Location Async" Click="button1_Click" 
            HorizontalAlignment="Left" Margin="11,164,0,0" Name="button1" VerticalAlignment="Top" />
        <TextBlock Margin="14,220,0,0" Text="Latitude" Name="textLatitude" />
        <TextBlock Margin="14,260,0,0" Text="Longitude" Name="textLongitude" />
        <TextBlock Margin="14,300,0,0" Text="Accuracy" Name="textAccuracy" />
        <TextBlock Margin="14,340,0,0" Text="UserID" Name="textUserID" />

        <ListView x:Name="MyList"
                    IsItemClickEnabled="True"
                    Margin="14,380,0,0"
                    ItemClick="ItemView_ItemClick"
                    ContinuumNavigationTransitionInfo.ExitElementContainer="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Background="Azure" Width="300">
                        <TextBlock Margin="14,10,0,0" Text="{Binding uid}" />
                        <TextBlock Margin="14,10,0,0" Text="{Binding content}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</Page>
